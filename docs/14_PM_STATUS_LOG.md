# PM 진척 체크 로그

2시간 주기로 GitHub와 노션의 실제 상태를 확인하고, 확인된 사실 기준으로 다음 작업을 추천한다. 이 문서는 사람을 추가로 배정하지 않는다. 이미 정해진 담당자가 다음에 무엇을 해야 하는지만 기록한다.

판정 근거는 GitHub 커밋·이슈·브랜치·PR과 노션 문서다. 구두 보고나 예상은 진척으로 인정하지 않는다.

---

## 2026-07-28 19:00 체크 (Day 1)

### 확인된 사실

| 항목 | 상태 |
|---|---|
| 커밋 | 14개, 전부 문서. 마지막 커밋 18:13 `docs: define Kim Gyeongeun design tasks` |
| 코드 | `backend/`, `frontend/` 디렉터리 없음. 실행 코드 0줄 |
| 이슈 | #1~#9 전부 OPEN, 담당자 9건 모두 미지정 |
| 브랜치 | `main` 1개. 역할별 브랜치 5종 미생성 |
| PR | 0건 |
| 19:00 리듬 | 브랜치 실행 결과·증거 제출 0건 |

### 진척 판정

**Day 1 게이트 기준 미달.** 오늘 23:00 게이트 통과 조건은 계약 동결과 fixture 기반 흐름 정의 완료인데, 문서 계약은 준비됐으나 이를 검증할 fixture와 실행 코드가 아직 없다. 지금까지의 산출물은 전부 설계 문서이며 병렬 작업이 아직 시작되지 않았다.

### 리스크

1. **최영 21:00 마감까지 2시간, 착수 흔적 없음.** `demo_state` 스키마, OpenAPI 초안, `demo_run.json`, `/api/health` 4종이 21:00 마감인데 커밋도 브랜치도 없다. 오늘 게이트 실패의 단일 최대 원인이 될 항목이다.
2. **fixture가 크리티컬 패스인데 아직 없다.** 준(#4 #7), 프론트엔드 담당(#4), 김경은 3명의 오늘 산출물이 `demo_run.json` 형식에 의존한다. 이 파일이 늦어지면 4명이 동시에 막힌다.
3. **이슈 담당자 전원 미지정.** GitHub에서 누가 무엇을 맡았는지 확인할 수 없어 진척 추적과 중복 작업 방지가 불가능하다.
4. **역할별 브랜치 미생성.** 병렬 작업 규칙이 문서에만 있고 저장소에 반영되지 않았다.

### 다음 작업 추천

우선순위 순서다. 위에서부터 처리한다.

| 순위 | 담당 | 다음 작업 | 근거 |
|---|---|---|---|
| 1 | 최영 | `demo_run.json` fixture를 스키마·API보다 **먼저** 커밋한다. 필드가 미완성이어도 7개 화면이 그릴 수 있는 최소 형태로 우선 푸시한다 | 4명의 병목 해소가 21:00 마감 4종 완결보다 팀 전체 진척에 크다 |
| 2 | 준 | Issue #1~#9 담당자 지정. 매핑은 노션 지시서 기준: #1 #2 #3 #5 #6 #9 최영, #4 준·프론트 공동, #7 준, #8 시우 | GitHub 웹에서 2분. 이후 모든 진척 추적의 전제 조건 |
| 3 | 준 | 시나리오 ID `dry_base`, `rain_spillback_a`, `rain_spillback_b` 확정을 팀 채팅에 선언 | docs/07의 정상·혼잡·사고 명칭과 충돌 중. 23:00 이후 변경 금지이므로 지금 선언해야 동결 가능 |
| 4 | 전원 | 역할별 브랜치 생성 후 첫 커밋 푸시: `feat/backend`(최영), `feat/frontend-core`(준), `feat/components`(프론트), `assets/design`(김경은), `docs/evidence`(시우) | 브랜치가 있어야 19:00·22:00 리듬에서 증거 제출이 가능 |
| 5 | 프론트엔드 담당 | fixture를 기다리지 말고 하드코딩 props로 KPI 카드 4종 목업 시작 | 노션 폴백 규칙에 명시된 독립 진행 경로. 1순위가 늦어도 오늘 산출물 확보 가능 |
| 6 | 시우 | provisional 시나리오 수치표와 안전 가드 임계값 초안을 `docs/evidence` 브랜치에 커밋 | 최영의 시뮬레이터 구현 착수 조건. 값은 나중에 교체 가능하므로 지금은 가정 표시만으로 충분 |
| 7 | 김경은 | `design-tokens.json` 상태 색상 13종을 먼저 커밋 | 아이콘·SVG보다 먼저 나와야 프론트 2명이 CSS 변수를 확정할 수 있음 |

### 다음 체크

2026-07-28 21:00. 확인 항목은 `demo_run.json` 존재 여부, 이슈 담당자 지정 완료 여부, 역할별 브랜치 생성 수다.

---

## 2026-07-28 21:13 체크 (Day 1)

19:00 체크가 지정한 확인 항목 3건을 대조한 결과다. 이번 체크부터 점검이 2시간 주기 로컬 예약 작업으로 전환됐다.

### 확인된 사실

| 항목 | 상태 |
|---|---|
| 커밋 | 15개. **19:00 체크 이후 신규 0개.** 마지막 실질 작업 커밋은 18:13 `docs: define Kim Gyeongeun design tasks`이고 `926c863`은 19:00 PM 로그 자체다 |
| 코드 | `backend/`, `frontend/` 디렉터리 없음. `demo_run.json` 없음. 실행 코드 0줄 |
| 21:00 마감 4종 | `demo_state` 스키마 없음, OpenAPI 초안 없음, `demo_run.json` 없음, `/api/health` 없음. **4종 전량 미납** |
| 이슈 | #1~#9 전부 OPEN. 이번 체크에서 #4·#7을 `sodam3156`에 지정. 나머지 7건은 계정 매핑 미확정으로 보류 |
| 브랜치 | `main` 1개. 역할별 브랜치 5종 미생성 |
| PR | 0건 |
| 19:00 리듬 | 실행 결과·증거 제출 0건 |

### 진척 판정

**19:00 이후 2시간 13분 동안 진척 0.** 신규 커밋·브랜치·PR이 모두 0건이고 21:00 마감 4종은 전량 미납이다.

Day 1 게이트(22:00~23:00) 통과 조건은 계약 동결과 fixture 기반 흐름 정의인데, 계약은 문서로 동결됐으나 이를 검증할 fixture가 없다. 게이트까지 남은 시간은 최대 1시간 47분이다. 이 시간 안에 4종을 모두 납품하는 것은 현실적이지 않으므로, 게이트 목표를 완결이 아니라 **최소 실물 1개 확보**로 낮추는 판단이 필요하다.

### 리스크

1. **21:00 마감 전량 미납으로 게이트 실패 경로에 진입했다.** 19:00 체크에서 최대 리스크로 지목한 항목이 그대로 실현됐다. 지금 필요한 것은 마감 재촉이 아니라 게이트에 제출할 최소 실물의 정의다.
2. **fixture 부재로 4명이 오늘 하루 종일 대기했다.** 준(#4 #7), 프론트엔드 담당(#4), 김경은의 오늘 산출물이 `demo_run.json` 형식에 의존한다. 대기 시간이 길어질수록 폴백 경로(하드코딩 props, 고정 규격 디자인)로 전환하는 편이 유리해진다.
3. **GitHub 계정 매핑이 불가능하다.** 이번 체크에서 새로 확인된 항목이다. collaborator는 `sodam3156`, `Wordmaster-digital`, `dugkdbs121-ui` 3명뿐인데 뒤 두 계정은 프로필 name·bio·company가 전부 비어 있어 실명 대응이 안 된다. 팀은 5명이므로 2명은 저장소 접근 권한 자체가 없다. 이 상태에서는 담당자 지정도 진척 추적도 불가능하다.
4. **역할별 브랜치가 없어 게이트에 제출할 증거 경로가 존재하지 않는다.** 22:00 게이트에서 각자 무엇을 보여줄지가 저장소상에 정의돼 있지 않다.
5. **제출 변형안이 확정되지 않았다.** core MVP의 합성 5개 신호교차로와 RainFlow 우천 회전교차로가 둘 다 정본처럼 기술돼 있다. 여기에 시나리오 ID 충돌(`docs/07`·이슈 #3의 정상·혼잡·사고 vs `docs/10`의 `dry_base` 계열)이 겹쳐 있다. 오늘 밤 동결 대상인데 미결이다.

### 다음 작업 추천

게이트까지 1시간 47분 기준으로 19:00 순위를 재조정했다. 목표는 완결이 아니라 게이트 방어다.

| 순위 | 담당 | 다음 작업 | 근거 |
|---|---|---|---|
| 1 | 최영 | `demo_run.json` **한 개 파일만** 푸시한다. 7개 화면이 그릴 수 있는 최소 필드면 충분하고 스키마·OpenAPI·`/api/health`는 내일로 미룬다 | 게이트에 제출할 유일한 실물이자 4명의 대기를 동시에 푸는 지점. 4종 완결보다 1종 실물이 오늘 밤 가치가 크다 |
| 2 | 준 | `Wordmaster-digital`, `dugkdbs121-ui`가 각각 누구인지 확정하고, 접근 권한이 없는 팀원 2명을 collaborator로 초대한다 | 이것 없이는 담당자 지정과 진척 추적이 남은 5일 내내 불가능하다. 소요 시간 5분 |
| 3 | 준 | 제출 변형안을 RainFlow 단일로 확정 선언하고, 시나리오 ID를 `dry_base`·`rain_spillback_a`·`rain_spillback_b`로 확정 선언한다 | 오늘 23:00 이후 변경 금지이므로 게이트 전에 선언해야 동결이 성립한다. `docs/07`의 정상·혼잡·사고는 화면 문구로만 남긴다 |
| 4 | 전원 | 역할별 브랜치를 만들고 빈 커밋이라도 푸시한다: `feat/backend`(최영), `feat/frontend-core`(준), `feat/components`(프론트), `assets/design`(김경은), `docs/evidence`(시우) | 브랜치가 있어야 게이트에서 증거를 제출할 자리가 생긴다. 각 1분 |
| 5 | 프론트엔드 담당 | fixture를 기다리지 말고 하드코딩 props로 KPI 카드 4종 목업을 시작한다 | 1순위가 오늘 밤 안에 안 나와도 오늘 산출물을 확보하는 독립 경로 |
| 6 | 김경은 | `design-tokens.json` 상태 색상 13종을 먼저 커밋한다 | 아이콘·SVG보다 먼저 나와야 프론트 2명이 CSS 변수를 확정할 수 있다 |
| 7 | 시우 | provisional 시나리오 수치표와 안전 가드 임계값 초안을 `docs/evidence`에 커밋한다 | 값은 나중에 교체 가능하므로 지금은 가정 표시만으로 충분하다 |

### 다음 체크

2026-07-28 21:52. 이후 2시간 주기로 자동 실행된다. 확인 항목은 `demo_run.json` 존재 여부, 역할별 브랜치 생성 수, 계정 매핑 확정 여부다. 23:52 체크에서 Day 1 게이트 판정 결과를 기록한다.

---

## 2026-07-28 21:30 체크 (Day 1)

노션 페이지 2건 모두 `object_not_found`(404)로 접근 실패했다. 저장소에 연결된 노션 통합이 두 페이지에 권한이 없거나 ID가 바뀐 것으로 보인다. 이번 체크는 저장소 문서(`docs/10`, `docs/12`, `docs/15`, `contracts/rainflow.schema.json`)만으로 진행한다.

또한 이전 체크 로그(19:00·21:13)가 "21:00 마감 4종"이라고 표기한 것은 `docs/10_TEAM_PARALLEL_EXECUTION.md`의 실제 게이트 표와 다르다. 문서상 최영 납품 게이트는 **7월 28일 14시**(상태 스키마·OpenAPI 초안·`demo_run.json`·health 응답)와 **7월 28일 22시**(FastAPI 골격과 계약 테스트, 스키마 동결 후보)이며 21시 게이트는 문서에 없다. 이번 체크부터 이 표를 기준으로 판정한다.

### 확인된 사실

| 항목 | 상태 |
|---|---|
| 커밋 | 18개. 21:13 체크 이후 신규 2개(`8f17fb1`, `4462191`, 둘 다 21:27). 내용은 `contracts/rainflow.schema.json`, `backend/fixtures/demo_run.json`, `docs/15_DAY1_FREEZE_DECISION.md` |
| 커밋 작성자 | 전체 18개 커밋이 전부 `JunJunJun`(준) 1인. 프로젝트 시작 이후 최영·시우·프론트엔드 담당·김경은 명의 커밋 0건 |
| `demo_run.json` | **존재.** 7개 화면 상태(`normal`~`recovery_compare`)를 모두 포함하는 provisional fixture. 21:13 체크 1순위 추천이 이행됨 |
| 계약 스키마 | **존재.** `contracts/rainflow.schema.json`. `docs/15`에서 시나리오 ID(`dry_base`/`rain_spillback_a`/`rain_spillback_b`), 정책 ID, KPI 4종을 동결 선언. 21:13 체크 3순위 추천이 이행됨 |
| 14시 게이트(4종) | 스키마 ✓, `demo_run.json` ✓, OpenAPI 초안 ✗ 없음, `/api/health` 응답 ✗ 없음. **4종 중 2종만 납품, 나머지 2종은 7시간 이상 지연 중** |
| 22시 게이트 | FastAPI 골격·계약 테스트 ✗ (`backend/app` 디렉터리 자체가 없음), 스키마 동결 후보 ✓. 게이트까지 약 30분 남음 |
| 브랜치 | `assets/design`, `docs/evidence`, `feat/backend`, `feat/components`, `feat/frontend-core` 5종 **생성됨**(21:13 체크 4순위 추천 부분 이행). 단 5개 전부 `main`과 동일 커밋(`4462191`)을 가리켜 **역할별 첫 커밋은 아직 0건** |
| 코드 | `backend/fixtures`만 존재. `backend/app`, `frontend/`, `release/` 전부 없음. 실행 가능한 백엔드·프론트 코드 0줄 |
| `design-tokens.json` | 없음 |
| 이슈 | #1~#9 전부 OPEN. #4는 `sodam3156`+`dugkdbs121-ui`, #7은 `sodam3156`에 지정. 나머지 7건(#1 #2 #3 #5 #6 #8 #9)은 21:13 체크와 동일하게 담당자 미지정 |
| collaborator | `sodam3156`, `Wordmaster-digital`, `dugkdbs121-ui` 3명. 21:13 체크와 동일. 팀 5명 중 2명은 여전히 저장소 접근 권한 없음 |
| PR | 0건 |

### 진척 판정

**Day 1 게이트 기준 미달, 계약 동결만 진행.** 21:13 체크 이후 17분 만에 `demo_run.json`·스키마·동결 결정 문서 3건이 나온 것은 실질 진척이나, 전부 준(JunJunJun) 단독 커밋이고 21:13 체크가 지목한 "최영" 담당 산출물을 준이 대신 채운 것이다. 22시 게이트의 핵심인 FastAPI 골격은 30분 앞두고 착수 흔적이 전혀 없어 이 게이트는 미달로 확정될 가능성이 높다.

### 리스크

1. **프로젝트 전체 기간 동안 준 1인 외 커밋 0건.** 18개 커밋 전부 `JunJunJun` 명의다. 최영·시우·프론트엔드 담당·김경은이 저장소에 어떤 형태로도 기여한 흔적이 없다. 이는 특정 게이트의 지연이 아니라 병렬 작업 자체가 시작되지 않았다는 신호다.
2. **22시 FastAPI 골격 게이트가 사실상 실패로 귀결될 것이다.** `backend/app` 디렉터리가 없고 30분 안에 골격과 계약 테스트를 새로 만드는 것은 비현실적이다. 준이 fixture와 스키마로 크리티컬 패스를 대신 풀었지만, 살아있는 API 없이는 22시 게이트 정의를 충족할 수 없다.
3. **역할별 브랜치가 이름만 존재한다.** 5개 브랜치가 전부 `main`과 동일 커밋을 가리켜 "브랜치가 있어야 증거 제출이 가능하다"는 21:13 추천의 목적(개인별 작업 증거)이 아직 달성되지 않았다.
4. **collaborator 미해결이 두 번째 체크에서도 그대로다.** 팀 5명 중 2명이 여전히 저장소 접근 권한이 없다. 이 상태가 유지되는 한 이슈 담당자 지정과 진척 추적은 구조적으로 불가능하고, 지금까지의 패턴(준 1인 커밋)이 계속될 가능성이 크다.
5. **시우·김경은·프론트엔드 담당의 독립 경로 산출물이 여전히 0건이다.** `demo_run.json`이 나온 지 몇 분 안 됐지만, provisional 수치표·`design-tokens.json`·하드코딩 KPI 카드 목업 중 어느 것도 커밋되지 않았다.

### 다음 작업 추천

크리티컬 패스(여러 사람을 동시에 막는 산출물)를 최우선으로 놓는다.

| 순위 | 담당 | 다음 작업 | 근거 |
|---|---|---|---|
| 1 | 준 | 저장소 접근 권한이 없는 나머지 팀원 2명을 collaborator로 초대하고, 이슈 #1 #2 #3 #5 #6 #9(최영)·#8(시우) 담당자를 매핑대로 지정한다 | 두 번째 체크에서도 미해결. 이것 없이는 준 1인 커밋 패턴이 8월 1일까지 이어질 위험이 가장 크다 |
| 2 | 최영 | `backend/app/main.py`에 `/api/health` 최소 골격만 먼저 커밋한다. OpenAPI 자동 문서·계약 테스트는 22시 게이트 이후로 미룬다 | 22시 게이트 핵심 항목이자 현재 유일하게 0%인 크리티컬 패스. 완결보다 살아있는 엔드포인트 1개가 지금 더 가치 있다 |
| 3 | 시우 | provisional 시나리오 수치표와 안전 가드 임계값 초안을 `docs/evidence` 브랜치에 커밋한다 | `docs/15`가 큐 모델을 Day 1~2 정식 경로로 확정했으므로 최영의 안전 가드 구현 착수 조건이다. 아직 0건 |
| 4 | 프론트엔드 담당 | `demo_run.json`이 이제 존재하므로 이를 props로 읽는 KPI 카드 4종을 `feat/components` 브랜치에 커밋한다 | fixture 대기 이유가 사라졌다. 지금부터는 순수 지연이다 |
| 5 | 김경은 | `design-tokens.json` 상태 색상 13종을 `assets/design` 브랜치에 커밋한다 | 아이콘·SVG보다 먼저 나와야 프론트 2명이 CSS 변수를 확정할 수 있다. 아직 0건 |
| 6 | 전원 | 배정된 역할 브랜치에 최소 1개 커밋을 남겨 브랜치를 `main`에서 분리시킨다 | 브랜치가 이름만 있고 실제 작업 증거가 없다. 각 1커밋이면 충분 |

### 다음 체크

2026-07-28 23:30. 확인 항목은 (1) 22시 FastAPI 골격 게이트 실제 통과 여부, (2) collaborator 초대 완료 여부, (3) 준 외 커밋 작성자 등장 여부, (4) 5개 역할 브랜치의 `main` 대비 분기 여부다.

---

## 2026-07-28 Day 1 게이트 판정 (준 야간 대행 작업 완료 기록)

준이 취침 전 야간 작업을 AI 대행에 위임했고, 그 결과를 확인된 커밋 기준으로 기록한다.

### 이번 판정에서 새로 확인된 사실

| 항목 | 상태 |
|---|---|
| 22시 게이트 (FastAPI 골격·계약 테스트) | **사후 충족.** PR #10 머지로 `backend/app` 전체(main.py, simulation.py, safety.py)와 테스트 20종이 main에 존재. `/api/health` 포함 동결 API 4종 구현 |
| 14시 게이트 잔여 2종 | OpenAPI는 FastAPI 자동 문서로, health 응답은 구현으로 해소 |
| 시뮬레이터 | 결정론적 큐 모델로 dry_base 무spillback, 우천 시나리오 spillback 재현. 시드 10개에서 스파이크 통과 기준(spillback 30%↓, TTT 10%↓, 공정성 가드) 자동 테스트 통과 |
| 안전 가드 | FAIRNESS_P95_EXCEEDED 등 코드화. 가드 위반 후보 승인은 API가 409로 거부하고 감사 로그에 기록 |
| 프론트 | PR #11 머지. `frontend/index.html` fixture 기반 7단계 3분 데모. 하드코딩 아닌 결과 JSON 표시 |
| PR | #10, #11 생성·머지 완료 (본문에 입력·출력·실행법·검증·폴백 기록) |
| 이슈 | #9를 `sodam3156`에 임시 지정하고 동결 선언·검증 결과 코멘트 2건 기록 |
| main 상태 | `python -m pytest backend/tests -q` 20/20 통과. fixture 모드 3분 시연 가능 조건 충족 |

### 판정

**Day 1 게이트 통과.** 계약 동결(docs/15, contracts/), fixture(demo_run.json), 실행 코드(백엔드+프론트), 재현성 테스트가 main에 모두 존재한다. 단 전 커밋이 준 1인(및 AI 대행) 명의라는 21:30 체크의 구조적 리스크는 해소되지 않았다.

### 사람이 해야 할 잔여 작업 (아침 최우선)

| 순위 | 담당 | 작업 |
|---|---|---|
| 1 | 준 | 나머지 팀원 2명 collaborator 초대, `Wordmaster-digital`·`dugkdbs121-ui` 실명 확정, 이슈 #1 #2 #3 #5 #6 담당자 지정 |
| 2 | 최영 | PR #10 코드 리뷰. 큐 모델 파라미터 검토 후 이슈 #9 클로즈 여부 결정 |
| 3 | 시우 | backend/README.md의 provisional 파라미터 표 검증, `docs/evidence`에 수치 근거 커밋 |
| 4 | 김경은 | `design-tokens.json` 커밋 → frontend/index.html의 임시 다크 테마 색상 교체 |
| 5 | 프론트엔드 담당 | frontend/index.html 컴포넌트 분리 및 API 어댑터 연결 (Day 2) |

---

## 2026-07-29 새벽 야간 대행 누적 체크 (Day 2)

준 위임 1시간 주기 AI 대행 루틴의 누적 결과다. 확인 근거는 전부 머지된 PR과 main 커밋이다.

### 확인된 사실

| 항목 | 상태 |
|---|---|
| 머지된 PR | #12 provisional 수치표, #13 design-tokens 13종, #14 프론트 live API 어댑터, #15 Windows 실행 스크립트, #16 **dugkdbs121-ui의 React 컴포넌트(첫 팀원 기여)**, #17 PyInstaller 실빌드 수정, #18 릴리스 패키징, #19 3분 데모 스크립트 |
| 게이트 진척 | 7/29 전 항목, 7/30(API 연동+자동 폴백), 7/31(exe 빌드·한글 경로·재실행 검증, 패키징 스크립트) 자동화 가능분 완료 |
| 번들 검증 | exe 6.6MB/번들 32MB. 저장소 밖 한글·공백 경로에서 health 200, live_simulation 응답, 재실행 무충돌 (docs/evidence/bundle_smoke_20260729.md) |
| 제출 패키징 | release/package_release.ps1 원커맨드: 데모 zip 16MB + 소스 zip + SHA256SUMS (docs/evidence/release_package_20260729.md) |
| main | 테스트 20/20, fixture 3분 시연 가능 유지 |
| collaborator | 초대 2건 발송됨(ilhsh, zcxsdasdas), 수락 대기 |

### 사람이 해야 할 잔여 작업

| 순위 | 담당 | 작업 |
|---|---|---|
| 1 | ilhsh·zcxsdasdas | collaborator 초대 수락 → 준이 이슈 #1 #2 #3 #5 #6 담당 지정 |
| 2 | 준 | docs/16 데모 스크립트를 소리 내 읽으며 타이밍 확정 |
| 3 | 프론트엔드 담당 | feat/components에 package.json+vite.config 추가해 React 앱 승격 (PR #16 리뷰 코멘트 참고) |
| 4 | 최영 | PR #10 #15 #17 #18 코드 리뷰, 이슈 #9 클로즈 판단 |
| 5 | 시우 | docs/evidence/provisional_parameters.md 문헌 대조 검증 |
| 6 | 전원(8/1) | 외부 PC 2대 실검증 + 백업 영상 촬영 |

---

## 2026-07-29 08:21 체크 (Day 2)

야간 대행 결과를 **실행 검증**으로 재확인한 체크다. 커밋 메시지와 PR 본문이 아니라 실제로 서버를 띄우고 테스트를 돌려 판정했다.

### 확인된 사실

| 항목 | 상태 |
|---|---|
| 커밋 | 45개. 7/28 21:13 체크(15개) 대비 **+30개**. 전 커밋이 `JunJunJun` 1인 명의 |
| PR | #10~#20 **11건 전부 MERGED**. 열린 PR 0건 |
| 브랜치 | 7개. `feat/backend`, `feat/frontend-core`, `feat/components`, `assets/design`, `docs/evidence`, `agent/*` 2종 |
| 백엔드 테스트 | `python -m pytest -q` → **42 passed**. 직접 실행해 확인 |
| 백엔드 기동 | uvicorn 기동 후 `GET /api/health` **200**. `version 0.2.0`, `fixture_available true`, `llm rule_based_fallback`. 직접 실행해 확인 |
| OpenAPI | 6개 경로 서빙. 동결 P0 API 4종 전부 존재 + `/api/scenarios`, `/api/audit/{run_id}` 추가(가산이므로 계약 위반 아님) |
| fixture 계약 정합 | `frontend/demo_run.js`와 `backend/fixtures/demo_run.json`이 **완전 동일**. 화면 상태 7/7, 정책 3/3, KPI 4/4 전부 포함. 드리프트 없음 |
| 프론트 실행물 | `frontend/index.html`(32KB) + `demo_run.js`(25KB) 독립 실행형. 백엔드 없이 로드 가능 |
| React 앱 | `frontend/src/*.tsx` 6개 존재하나 **`package.json`·`vite.config` 없음 → 빌드도 실행도 불가** |
| 릴리스 번들 | `release/start.bat`, `package_release.ps1`, `.zip.sha256`, PyInstaller spec, `launcher/` 존재 |
| collaborator | 3명. `ilhsh`(최영)·`zcxsdasdas`(김경은) **여전히 초대 미수락** |
| 이슈 | #4 준+여하윤, #7 준, #8 시우, #9 준. **#1 #2 #3 #5 #6 미지정**(최영 계정 접근 불가) |
| 저장소 위생 | 루트에 수동 ingest 부산물 4개 커밋됨: `TRACKED_CHANGES.patch`(158KB), `CHANGED_FILES_MANIFEST.txt`, `CHANGESET_README.md`, `GIT_STATUS.txt` |

### 진척 판정

**Day 2 게이트 통과.** 7월 29일 기준은 백엔드가 Swagger에서, 프론트가 fixtures에서 각각 독립 완주하는 것이다. 백엔드는 실제 기동과 42개 테스트로, 프론트는 독립 실행형 fixture 번들로 각각 충족한다. 7/30(API 연동·자동 폴백)과 7/31(Windows 번들) 항목도 상당 부분 선행 완료돼 **일정상 하루 이상 앞서 있다.**

다만 통과의 성격을 분명히 해 둔다. 진척의 원인은 병렬 작업 복구가 아니라 준이 최영·경은의 파일을 수동으로 받아 대신 커밋한 것이다. 산출물은 들어왔지만 팀 구조는 여전히 1인 병목이다.

남은 시간을 신규 구현이 아니라 **검증과 정리**에 쓸 수 있는 상태이며, 아래 리스크 1·2는 지금 처리하지 않으면 제출물에 그대로 남는다.

### 리스크

1. **동결 스택과 실제 실행물이 어긋난다.** `ai-context/PROJECT_STACK.yaml`과 `docs/12`는 React·Vite·TypeScript를 동결했는데, 3분 데모를 실제로 돌리는 것은 바닐라 `frontend/index.html`이다. `frontend/src/`의 컴포넌트 6개는 `package.json`이 없어 빌드조차 되지 않는 죽은 코드다. 지금 상태는 최악의 조합이다. 두 구현이 공존하는데 어느 쪽도 정본이 아니고, 여하윤이 PR #16으로 낸 컴포넌트가 제품에 반영되지 않는다. 심사에서 스택을 설명할 때 문서와 실물이 불일치한다.
2. **제출물에 개발 부산물이 섞여 있다.** 루트의 `TRACKED_CHANGES.patch` 158KB를 비롯한 4개 파일은 수동 ingest 과정의 중간 산출물이다. public 저장소에 노출돼 있고 제출 소스 zip에도 그대로 들어간다.
3. **팀원 2명 접근 차단이 사흘째다.** `ilhsh` 초대는 07-27 20:23 발송 후 36시간, `zcxsdasdas`는 14시간 경과다. 45개 커밋 전부가 준 1인 명의이며 기여 이력·리뷰·병렬 작업이 모두 준에게 집중돼 있다. 수동 파일 전달이 우회로로 작동하고 있으나, 이 방식은 리뷰 단계가 구조적으로 존재할 수 없다.
4. **백엔드 전량이 리뷰 없이 main에 있다.** 큐 모델 파라미터와 안전 가드 임계값이 최영 검증을 거치지 않았다. 42개 테스트는 구현이 스스로 정한 기준을 통과했다는 뜻이지 파라미터가 타당하다는 근거가 아니다. 시우의 provisional 수치도 아직 문헌 대조 전이다.
5. **`backend/fixtures/cached_run.json`의 `scenario_id`가 `None`이다.** 캐시 폴백 경로에서 시나리오 식별이 안 될 여지가 있다. 우선순위는 낮으나 폴백 시연 시 확인이 필요하다.

### 다음 작업 추천

| 순위 | 담당 | 다음 작업 | 근거 |
|---|---|---|---|
| 1 | 준 | 프론트 정본을 **오늘 중 하나로 결정한다.** React로 승격하려면 `package.json`+`vite.config`를 추가하고, 안 할 거면 `frontend/src/`를 제거하고 `PROJECT_STACK.yaml`·`docs/12`를 실제 실행물에 맞춘다 | 둘 다 두는 선택지는 없다. 제출까지 4일 남았고 문서와 실물이 다른 상태로 심사에 들어가면 스택 질문에 답할 수 없다 |
| 2 | 준 | 루트 ingest 부산물 4개 삭제: `TRACKED_CHANGES.patch`, `CHANGED_FILES_MANIFEST.txt`, `CHANGESET_README.md`, `GIT_STATUS.txt` | 제출 소스 zip에 들어간다. 1분 작업 |
| 3 | 준 | 최영·경은에게 초대 수락 링크 직접 전달 | 리뷰 단계를 만들려면 필수다. 수동 전달은 산출물은 옮기지만 리뷰는 옮기지 못한다 |
| 4 | 최영 | 백엔드 큐 모델 파라미터와 안전 가드 임계값 검토 후 #9 클로즈 판단 | 리스크 4의 유일한 해소 경로. 코드는 이미 main에 있으므로 읽기만 하면 된다 |
| 5 | 시우 | `docs/evidence/provisional_parameters.md` 문헌 대조 검증 후 provisional 표시 해제 | 발표에서 수치 근거 질문이 나오면 현재는 답할 수 없다 |
| 6 | 여하윤 | 1순위 결정에 따라 컴포넌트를 실제 실행 경로에 연결하거나 index.html 유지보수로 전환 | 현재 산출물이 제품에 반영되지 않고 있다 |
| 7 | 경은 | `design-tokens.json` 최종 확정 및 `index.html` 임시 색상 교체 | 토큰은 들어왔으나 실행물에 미반영 |

### 다음 체크

2026-07-29 09:47 자동 실행. 확인 항목은 프론트 정본 결정 여부, 루트 부산물 정리 여부, `ilhsh`·`zcxsdasdas` 초대 수락 여부다.
