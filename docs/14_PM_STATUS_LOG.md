# PM 진척 체크 로그

2시간 주기로 GitHub와 노션의 실제 상태를 확인하고, 확인된 사실 기준으로 다음 작업을 추천한다. 이 문서는 사람을 추가로 배정하지 않는다. 이미 정해진 담당자가 다음에 무엇을 해야 하는지만 기록한다.

판정 근거는 GitHub 커밋·이슈·브랜치·PR과 노션 문서다. 구두 보고나 예상은 진척으로 인정하지 않는다.

---

## 2026-07-28 19:00 체크 (Day 1)

### 확인된 사실

| 항목 | 상태 |
|---|---|
| 커밋 | 14개, 전부 문서. 마지막 커밋 18:13 `docs: define Kim Gyeongeun design tasks` |
| 코드 | `backend/`, `frontend/` 디렉터리 없음. 실행 코드 0줄 |
| 이슈 | #1~#9 전부 OPEN, 담당자 9건 모두 미지정 |
| 브랜치 | `main` 1개. 역할별 브랜치 5종 미생성 |
| PR | 0건 |
| 19:00 리듬 | 브랜치 실행 결과·증거 제출 0건 |

### 진척 판정

**Day 1 게이트 기준 미달.** 오늘 23:00 게이트 통과 조건은 계약 동결과 fixture 기반 흐름 정의 완료인데, 문서 계약은 준비됐으나 이를 검증할 fixture와 실행 코드가 아직 없다. 지금까지의 산출물은 전부 설계 문서이며 병렬 작업이 아직 시작되지 않았다.

### 리스크

1. **최영 21:00 마감까지 2시간, 착수 흔적 없음.** `demo_state` 스키마, OpenAPI 초안, `demo_run.json`, `/api/health` 4종이 21:00 마감인데 커밋도 브랜치도 없다. 오늘 게이트 실패의 단일 최대 원인이 될 항목이다.
2. **fixture가 크리티컬 패스인데 아직 없다.** 준(#4 #7), 프론트엔드 담당(#4), 김경은 3명의 오늘 산출물이 `demo_run.json` 형식에 의존한다. 이 파일이 늦어지면 4명이 동시에 막힌다.
3. **이슈 담당자 전원 미지정.** GitHub에서 누가 무엇을 맡았는지 확인할 수 없어 진척 추적과 중복 작업 방지가 불가능하다.
4. **역할별 브랜치 미생성.** 병렬 작업 규칙이 문서에만 있고 저장소에 반영되지 않았다.

### 다음 작업 추천

우선순위 순서다. 위에서부터 처리한다.

| 순위 | 담당 | 다음 작업 | 근거 |
|---|---|---|---|
| 1 | 최영 | `demo_run.json` fixture를 스키마·API보다 **먼저** 커밋한다. 필드가 미완성이어도 7개 화면이 그릴 수 있는 최소 형태로 우선 푸시한다 | 4명의 병목 해소가 21:00 마감 4종 완결보다 팀 전체 진척에 크다 |
| 2 | 준 | Issue #1~#9 담당자 지정. 매핑은 노션 지시서 기준: #1 #2 #3 #5 #6 #9 최영, #4 준·프론트 공동, #7 준, #8 시우 | GitHub 웹에서 2분. 이후 모든 진척 추적의 전제 조건 |
| 3 | 준 | 시나리오 ID `dry_base`, `rain_spillback_a`, `rain_spillback_b` 확정을 팀 채팅에 선언 | docs/07의 정상·혼잡·사고 명칭과 충돌 중. 23:00 이후 변경 금지이므로 지금 선언해야 동결 가능 |
| 4 | 전원 | 역할별 브랜치 생성 후 첫 커밋 푸시: `feat/backend`(최영), `feat/frontend-core`(준), `feat/components`(프론트), `assets/design`(김경은), `docs/evidence`(시우) | 브랜치가 있어야 19:00·22:00 리듬에서 증거 제출이 가능 |
| 5 | 프론트엔드 담당 | fixture를 기다리지 말고 하드코딩 props로 KPI 카드 4종 목업 시작 | 노션 폴백 규칙에 명시된 독립 진행 경로. 1순위가 늦어도 오늘 산출물 확보 가능 |
| 6 | 시우 | provisional 시나리오 수치표와 안전 가드 임계값 초안을 `docs/evidence` 브랜치에 커밋 | 최영의 시뮬레이터 구현 착수 조건. 값은 나중에 교체 가능하므로 지금은 가정 표시만으로 충분 |
| 7 | 김경은 | `design-tokens.json` 상태 색상 13종을 먼저 커밋 | 아이콘·SVG보다 먼저 나와야 프론트 2명이 CSS 변수를 확정할 수 있음 |

### 다음 체크

2026-07-28 21:00. 확인 항목은 `demo_run.json` 존재 여부, 이슈 담당자 지정 완료 여부, 역할별 브랜치 생성 수다.
