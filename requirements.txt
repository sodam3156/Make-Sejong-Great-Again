# Runtime dependencies used by the FastAPI application and packaged executable.
# Python support is intentionally limited to the project contract: >=3.11,<3.13.
# Keep FastAPI and Starlette in the minor versions exercised by the 3.11/3.12
# verification matrix. Starlette 1.3 uses HTTPX2 for TestClient (dev dependency).
fastapi>=0.141.1,<0.142
starlette>=1.3.1,<1.4
pydantic>=2.8,<3.0
uvicorn>=0.30,<1.0
